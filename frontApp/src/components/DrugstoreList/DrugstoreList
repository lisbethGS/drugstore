import React from 'react';

export default class DrugstoreList extends React.Component {
  props = {
    drugstores: []
  }
  
  render() {
    return (
      <ul>
        { this.state.drugstores.map(drugstore => <li>{drugstore.name}</li>)}
      </ul>
    )
  }
}