import React from 'react';

export default class DrugstoreList extends React.Component {
  props = {
    drugstores: []
  }
  
  render() {
    return (
      <ul>
        { this.props.drugstores.map(drugstore => <li>{drugstore.NombreLocal}</li>)}
      </ul>
    )
  }
}